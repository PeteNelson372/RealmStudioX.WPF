<UserControl x:Class="RealmStudioX.WPF.Views.Panels.OverlaysToolPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="922" d:DesignWidth="152">

    <Border Background="#FFDFDBD2"
            Padding="10">

        <Grid Width="136" Margin="3,20,3,3" HorizontalAlignment="Center">

            <ListBox Grid.Row="1"
                 ItemsSource="{Binding FrameGridItems}"
                 SelectedItem="{Binding SelectedFrameGridItem}"
                 BorderThickness="0"
                 Background="Transparent"
                 Margin="3,3,3,3"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                <!-- Single vertical column -->
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <!-- Remove default ListBoxItem padding -->
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="2"/>
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>

                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Border"
                                        BorderBrush="Gray"
                                        BorderThickness="1"
                                        Background="Transparent">

                                        <ContentPresenter/>
                                    </Border>

                                    <ControlTemplate.Triggers>

                                        <!-- Selected -->
                                        <Trigger Property="IsSelected" Value="True">

                                            <Setter TargetName="Border"
                                                Property="Background"
                                                Value="#4488CCFF"/>

                                            <Setter TargetName="Border"
                                                Property="BorderBrush"
                                                Value="#66AAFFFF"/>

                                            <Setter TargetName="Border"
                                                Property="BorderThickness"
                                                Value="2"/>

                                            <Setter Property="Margin" Value="3"/>
                                        </Trigger>

                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <!-- Frame item -->
                <ListBox.ItemTemplate>
                    <DataTemplate>

                        <Grid Width="100" Height="60" Margin="3,3,3,3">
                            <Image Source="{Binding Thumbnail}" Stretch="Fill"/>
                        </Grid>

                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>

        </Grid>

    </Border>

</UserControl>
