<UserControl x:Class="RealmStudioX.WPF.Views.Controls.NumericUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:RealmStudioX.WPF.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="100">
            
        <Border BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
            BorderThickness="1"
            Background="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Padding="1">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <!-- narrower -->
            </Grid.ColumnDefinitions>

            <!-- Text -->
            <TextBox x:Name="ValueBox"
                BorderThickness="0"
                Background="Transparent"
                Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                HorizontalContentAlignment="Right"
                VerticalContentAlignment="Center"
                Padding="2"                     
                PreviewTextInput="OnPreviewTextInput"
                LostFocus="OnLostFocus"
                GotKeyboardFocus="OnTextBoxGotFocus"
                PreviewKeyDown="OnKeyDown"/>

            <!-- Buttons -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <!-- Up -->
                <Border Grid.Row="0"
                        Background="Transparent"
                        MouseDown="OnIncrease">

                    <materialDesign:PackIcon Kind="ChevronUp"
                                             Width="12"
                                             Height="12"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"
                                             Opacity="0.8"/>
                </Border>

                <!-- Down -->
                <Border Grid.Row="1"
                        Background="Transparent"
                        MouseDown="OnDecrease">

                    <materialDesign:PackIcon Kind="ChevronDown"
                                             Width="12"
                                             Height="12"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"
                                             Opacity="0.8"/>
                </Border>

            </Grid>

        </Grid>

    </Border>
</UserControl>
