<UserControl x:Class="RealmStudioX.WPF.Views.Controls.MainMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RealmStudioX.WPF.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="32" d:DesignWidth="1304">
    
    <Menu Background="#FFDFDBD2"
        Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
        Height="32">

        <MenuItem Header="_File" Margin="10,6,0,0">
            <MenuItem Header="_New/Open" Command="{Binding NewOpenCommand}"/>
            <MenuItem Header="_Save" Command="{Binding SaveCommand}"/>

            <Separator/>

            <MenuItem Header="_Export..." Command="{Binding ExportCommand}"/>
            
            <Separator/>

            <MenuItem Header="E_xit" Command="{Binding ExitCommand}"/>

        </MenuItem>

        <MenuItem Header="_Edit" Margin="0,6,0,0">
            <MenuItem Header="_Undo" Command="{Binding UndoCommand}" InputGestureText="Ctrl+Z"/>
            <MenuItem Header="_Redo" Command="{Binding RedoCommand}" InputGestureText="Ctrl+Y"/>
            <Separator/>
            <MenuItem Header="Cu_t" Command="{Binding CutCommand}" InputGestureText="Ctrl+X"/>
            <MenuItem Header="_Copy" Command="{Binding CopyCommand}" InputGestureText="Ctrl+C"/>
            <MenuItem Header="_Paste" Command="{Binding PasteCommand}" InputGestureText="Ctrl+V"/>
            <MenuItem Header="Clea_r Selection" Command="{Binding ClearSelectionCommand}" InputGestureText="Ctrl+R"/>
        </MenuItem>

        <MenuItem Header="Realm" Margin="0,6,0,0">
            <MenuItem Header="_Realm Properties" Command="{Binding OpenRealmPropertiesCommand}"/>
            <MenuItem Header="Selected _Object Properties" Command="{Binding OpenObjectPropertiesDialogCommand}"/>
            <MenuItem Header="Change Map _Size" Command="{Binding ChangeMapSizeCommand}"/>
            <MenuItem Header="Create _Detail Map" Command="{Binding CreateDetailMapCommand}"/>
            <MenuItem Header="_Trace Outline" Command="{Binding TraceOutlineCommand}"/>
            <Separator/>
            <MenuItem Header="Display _Height Map" Command="{Binding DisplayHeightMapCommand}" IsCheckable="True" IsChecked="{Binding RenderHeightMap, Mode=OneWay}"/>
            <MenuItem Header="Select _Height Map Color Palette..." Command="{Binding SelectHeightMapPaletteCommand}"/>
            <Separator/>
            <MenuItem Header="Display World _Globe..." Command="{Binding DisplayWorldGlobeCommand}"/>
        </MenuItem>

        <MenuItem Header="Assets" Margin="0,6,0,0">
            <MenuItem Header="_Reload All Assets" Command="{Binding ReloadAssetsCommand}"/>            
            <MenuItem Header="_Create Symbol Collection..." Command="{Binding CreateSymbolCollectionCommand}"/>
            <MenuItem Header="_Import Assets From">
                <MenuItem Header="Wonderdraft Asset _Zip File..." Command="{Binding ImportWonderdraftZipCommand}"/>
                <MenuItem Header="Wonderdraft _User Folder..." Command="{Binding ImportWonderdraftUserFolderCommand}"/>
            </MenuItem>
            <MenuItem Header="Import _GIMP Brush..." Command="{Binding OpenGimpBrushCommand}"/>
        </MenuItem>

        <MenuItem Header="Theme" Margin="0,6,0,0" Command="{Binding OpenThemeDialogCommand}">
        </MenuItem>

        <MenuItem Header="Options" Margin="0,6,0,0">
            <MenuItem Header="_Name Generator Configuration..." Command="{Binding OpenNameGeneratorConfigCommand}"/>
            <MenuItem Header="_Preferences..." Command="{Binding OpenPreferencesCommand}"/>            
        </MenuItem>

        <MenuItem Header="Help" Margin="0,6,0,0">
            <MenuItem Header="_Contents" Command="{Binding OpenHelpContentsCommand}"/>
            <Separator/>
            <MenuItem Header="Check For New _Release" Command="{Binding CheckNewReleaseCommand}"/>
            <Separator/>
            <MenuItem Header="_About" Command="{Binding OpenAboutCommand}"/>
        </MenuItem>

    </Menu>
</UserControl>
