<ui:ModelessDialog x:Class="RealmStudioX.WPF.Views.Dialogs.ThreeDModelViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:ui="clr-namespace:RealmStudioX.WPF.Editor.UserInterface"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"        
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:views="clr-namespace:RealmStudioX.WPF.Views"
        xmlns:controls="clr-namespace:RealmStudioX.WPF.Views.Controls"
        xmlns:vm="clr-namespace:RealmStudioX.WPF.ViewModels.Dialogs"
        xmlns:threeD="clr-namespace:RealmStudioX._3D.Views.Controls;assembly=RealmStudioX.3D"
        mc:Ignorable="d"
        Title="3D Model Viewer"
        Width="1000"
        Height="800"
        Background="#FF45443F"
        WindowStyle="None"
        ShowInTaskbar="True"
        ResizeMode="CanResize"
        Icon="pack://application:,,,/Assets/ico/realm_studio_icon.ico">

    <!-- WindowChrome -->
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="32"
                            ResizeBorderThickness="5"
                            CornerRadius="0"/>
    </shell:WindowChrome.WindowChrome>

    <Grid Background="#FFDFDBD2">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <!-- Custom Title Bar -->
            <RowDefinition Height="Auto"/>
            <!-- Menu -->
            <RowDefinition Height="*"/>
            <!-- Main -->
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ========================= -->
        <!-- CUSTOM TITLE BAR -->
        <!-- ========================= -->
        <Border Grid.Row="0"
            Grid.ColumnSpan="2"
            Background="#FF45443F">

            <controls:ThreeDViewTitleBar x:Name="TitleBar"/>

        </Border>

        <controls:ThreeDMenu x:Name="ThreeDMenu"
                Grid.Row="1"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Background="#FFDFDBD2"
                Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                Height="32"/>

        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Vertical" Margin="10,10,10,10">
            <threeD:ModelViewerControl x:Name="ModelViewer" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        </StackPanel>

    </Grid>
</ui:ModelessDialog>
